import React from "react";

import styles from "./Menu.module.scss";

interface MenuProps {}
const Menu = ({}: MenuProps) => {
  return <div>Menu. Ainda não implementado. </div>;
};

export default Menu;
